{
  // TODO: uzyc process.yml
  "apps": [
    {
      "name": "rest-api",
      "script": "main.js",
    }
  ],
  "deploy": {
    "production-aws": {
      "user": "node",
      "host": [
        "52.57.120.91"
      ],
      "ref": "origin/master",
      "repo": "https://github.com/michal-zg/gs.git",
      "path": "/var/lib/game-scheduler/production",
      "ssh_options": "StrictHostKeyChecking=no",
      "pre-deploy-local": "echo 'GS deploy start.'",
      "post-deploy": "npm install && tsc -p tsconfig.json && pm2 startOrRestart ecosystem.json --env production-aws",
      "env": {
        "NODE_ENV": "production-aws",
        "port": "3000",
        "mongodb": "mongodb://localhost:27017/game-scheduler"
      }
    }
  }
}